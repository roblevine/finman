# Finman User Service - Environment Configuration Example
# Copy this file to .env and adjust values as needed

# Database Configuration
# Default PostgreSQL connection string for local development
# Ensure PostgreSQL is running via docker-compose up postgres
DefaultConnection="Host=localhost;Port=5432;Database=finman_user_service;Username=finman_user;Password=finman_password"

# Migration Settings  
# Set to true to automatically apply EF Core migrations on startup in Development
# WARNING: Only use in Development - never in Production
MIGRATE_AT_STARTUP=false

# Logging Configuration
# Set to Debug for more detailed EF Core SQL logging
Logging__LogLevel__Microsoft.EntityFrameworkCore.Database.Command=Information

# Example usage:
# 1. Copy this file: cp .env.example .env  
# 2. Start PostgreSQL: docker-compose up -d postgres
# 3. Enable auto-migration: Set MIGRATE_AT_STARTUP=true in .env
# 4. Run application: ./scripts/run.sh --local
